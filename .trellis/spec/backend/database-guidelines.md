# Database Guidelines

**N/A** — This project has no database layer. It is a static HTML/CSS/JS generator with Python scripting. All data is read from files (HTML, Markdown, YAML, JSON) and processed in memory.

No ORM, no migrations, no query patterns.
