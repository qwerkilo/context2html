# Type Safety

**N/A** — No TypeScript. This is pure JavaScript without any type system.

No JSDoc type annotations either. The project intentionally has no build step (pure HTML/CSS/JS, open directly in browser).
